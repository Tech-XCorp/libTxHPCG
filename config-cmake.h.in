#ifndef LIB_TX_HPCG_CONFIG_H
#define LIB_TX_HPCG_CONFIG_H

// defined if we have MPI
#cmakedefine HAVE_MPI

// defined if we have GPU aware MPI
#cmakedefine HAVE_GPU_AWARE_MPI

#cmakedefine HPCG_NOMPI

// Whether we have the CUDA backend.
#cmakedefine HAVE_CU_BACKEND

// The optimized backend to use (see BackendFactory.hpp for available choices)
#cmakedefine OPTIMIZED_BACKEND_NAME "@OPTIMIZED_BACKEND_NAME@"

#endif
